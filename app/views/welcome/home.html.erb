<% if user_signed_in? %>
	<div class="col-md-4">
		<%= render "users/shared/devise_errors" %>
	</div>
	<%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
<% else %>
	<div class="container-fluid home-page-splash">
		<div class="row">
			<div class="col-md-4 col-md-offset-4">
				<%= render "users/shared/devise_errors" %>
			</div>
			<div class="col-md-8 col-md-offset-4">
				<h1 id="main-header">Run with friends to get PR's!</h1>
				<h2>
					<a href="/users/sign_in"><span class="label label-success">Login</span></a>
					<span></span>
					<a href="/users/sign_up"><span class="label label-primary">Register</span></a>
				</h2>
			</div>
		</div>
		<
		
	</div>
	<%= link_to('Login', new_user_session_path)  %>
<% end %>
